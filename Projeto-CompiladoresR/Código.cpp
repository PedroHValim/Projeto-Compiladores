#include <iostream> 
#include <string> 

int main() {
    int x = 10;
    int y = 5;
    int z = x + y * 2; 
    std::string Pedro = "meu nome é pedro"; 
    x = x + 1;
    y = y / 2 + 3; 
    if (x > y) { std::cout << x << std::endl;}else{std::cout << y << std::endl;}
    for (int i = 0; i < 5; i++) {if (i * 2 > 5) {std::cout << i << std::endl;} else {std::cout << "menor" << std::endl; }}
    while (x > 0) {x = x - 1;}
    {std::cout << "acabou" << std::endl;}
    
    std::string nome;
    std::cout << "Digite seu nome: ";
    std::cin >> nome;
    std::cout << "Olá" << std::endl; 

    return 0;
}


int x = 10 ;
 int y = 5 ;
 int z = x + y * 2 ;
 std::string Pedro = "meu nome � pedro" ;
 x = x + 1 ;
 y = y / 2 + 3 ;
 if ( x > y ) { std::cout << x <<std::endl ;} else { std::cout << y <<std::endl ;} 
 for( int i =  0 ; i <  5 ; i++) { if ( i * 2 > 5 ) { std::cout << i <<std::endl ;} else { std::cout << "menor" <<std::endl ;} } 
 while ( x > 0 ) { x = x - 1 ;} else { std::cout << "acabou" <<std::endl ;
 } int nome = input ;
 ;
 std::cout << "Ol�" <<std::endl ;